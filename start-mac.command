#!/bin/bash
# เปิดระบบเอง (กรณีไม่ได้ใช้ตัวเปิดอัตโนมัติ หรือสั่งปิดไปก่อนหน้า)
# ดับเบิลคลิกไฟล์นี้ใน Finder ได้เลย
cd "$(dirname "$0")"
echo "▶ กำลังเปิดระบบบริหารงานขนส่ง..."
echo "  ปิดหน้าต่างนี้ = ปิดเว็บ (ฐานข้อมูลยังทำงานต่อ)"
echo
exec bash scripts/mac/run-server.sh
