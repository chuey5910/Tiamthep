# หลักการของระบบนี้ (อ่านก่อนแก้ทุกครั้ง)

ระบบนี้สร้างมาเพื่อ **ช่วยให้ทำงานง่าย สำเร็จ รวดเร็ว ถูกต้อง และแม่นยำ**
ทุกอย่างที่เพิ่มเข้ามาต้องผ่าน 5 ข้อนี้ ถ้าขัดข้อใดข้อหนึ่ง ให้คิดใหม่

## 1. ง่าย — คนใช้ไม่ต้องจำ
- งานที่ทำด้วยกันเป็นชุด ต้องอยู่หน้าเดียวกัน (เช่น คลังอะไหล่: รายการ + รับเข้า + เบิกใช้)
- ปุ่มอยู่ตรงที่ผู้ใช้กำลังมอง — กดที่แถวของสิ่งนั้น ไม่ใช่ไปเลือกซ้ำอีกหน้า
- ตัวเลือกในรายการเรียงแบบที่คนไทยหาเจอ (ไทยก่อน อังกฤษทีหลัง) และมีช่องค้นหาเสมอเมื่อรายการยาว

## 2. เข้าใจง่าย — เห็นแล้วรู้เลย ไม่ต้องตีความ
- ข้อความสถานะใช้ **รูปแบบเดียวกันทุกแถว** ขึ้นต้นด้วยเครื่องหมายเดียวกัน (✅ / ⚠️ / ❌)
- บอกว่า "ต้องไปแก้ที่ไหน" ไม่ใช่แค่ "ผิด"
- ตัวเลขทุกตัวต้องกดดูที่มาได้ หรือมีสูตรกำกับ (เช่น `0.261 × 30,540 กก.`)
- หัวข้อใหญ่ชัด ตัวอักษรเข้ม ข้อย่อยมี – นำหน้า

## 3. รวดเร็ว — ลดจำนวนคลิกและการพิมพ์ซ้ำ
- อะไรที่ระบบรู้อยู่แล้ว ต้องเติมให้เอง (พขร. จากตารางจับคู่ · ราคาจากตารางเส้นทาง · ทุนล่าสุดของอะไหล่)
- แก้ข้อมูลแล้ว ผลต้องอัปเดตเองในรอบถัดไป ไม่ต้องไล่แก้ทีละแถว (เช่น ข้อความในชีตรีเฟรชเอง)
- งานที่ทำซ้ำได้ ต้องกดซ้ำได้โดยไม่เกิดข้อมูลซ้ำ

## 4. ถูกต้อง — ข้อมูลจริงเท่านั้น
- ห้ามให้ระบบเดาแทนคน ถ้าข้อมูลไม่ครบให้ขึ้นเตือนพร้อมบอกวิธีแก้
- ห้ามบังคับให้ผู้ใช้กรอกข้อมูลหลอกเพื่อให้ผ่าน (เช่น ใส่ทะเบียนหางปลอมให้รถเดี่ยว) — ต้องมีตัวเลือกที่ตรงความจริง
- ตัวเลขเงินทุกตัวเดินผ่าน `src/lib/calc.ts` ที่เดียว รายงานจึงไม่มีทางขัดกันเอง

## 5. แม่นยำ — เงินต้องไม่ผิดเด็ดขาด
- ก่อนส่งงานที่แตะตัวเลขเงิน ต้องทดสอบด้วยข้อมูลจริงและเทียบผลลัพธ์ที่คำนวณมือได้
- ตั้งค่าที่ผิดแล้วเงียบ ต้องมีตัวดัก (เช่น เตือนเมื่อหน่วยคิดราคาเป็น "ต่อเที่ยว" แต่ราคาต่ำผิดปกติทั้งที่งานมีน้ำหนัก)
- ข้อมูลซ้ำที่ทำให้จับคู่ผิดได้ ต้องขึ้นเตือนให้รวม (เช่น เส้นทางซ้ำที่สะกดต่างกัน)

---

## ข้อควรรู้ทางเทคนิค
- เครื่องจริงรันบน NAS ที่บ้าน: `/volume1/docker/tiamthep/app` · อัปเดตด้วย `sudo bash scripts/nas/tiamthep.sh update`
- ชีตสั่งงาน + ไลน์บอทอยู่บน Google (`line-bot/Code.gs`) — **ต้องมีเว็บเครื่องเดียวที่เชื่อมชีต** ไม่งั้นแย่งกันเขียนสถานะ
- แก้โครงคอลัมน์ในชีต ต้องแก้ทั้ง `src/lib/sheet-jobs.ts` และ `line-bot/Code.gs` ให้ตรงกัน
- สำรองข้อมูลอัตโนมัติ 00:00 และ 12:15 (กล่อง `tiamthep-backup` รัน `scripts/backup-daemon.ts`)
  เก็บทั้งในเครื่องและบน Google Drive 30 วัน แล้วแจ้งผลเข้าไลน์ถึงผู้ดูแลทุกรอบ
- ทุกการแก้ต้องผ่าน `npx tsc --noEmit` และทดสอบในเบราว์เซอร์จริงก่อน push
